<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Room List</title>
</head>
<body>
    <h1>Room List</h1>
    <ul>
        <% rooms.forEach(room => { %>
            <li><%= room.name %> - Capacity: <%= room.capacity %>, Location: <%= room.location %></li>
        <% }); %>
    </ul>
</body>
</html>
